{{- if and .Values.ErieCanal.ingress.enabled (semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion) }}
{{- if not .Values.ErieCanal.repo.preProvision.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pipy-repo
  namespace: {{ include "ErieCanal.namespace" . }}
  annotations:
    pipy.ingress.kubernetes.io/rewrite-target-from: ^/mesh/?
    pipy.ingress.kubernetes.io/rewrite-target-to: /
  labels:
    {{- include "ErieCanal.labels" . | nindent 4 }}
spec:
  {{- if .Values.ErieCanal.ingress.className }}
  ingressClassName: {{ .Values.ErieCanal.ingress.className }}
  {{- end }}
  rules:
  - http:
      paths:
      - path: /mesh
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.ErieCanal.services.repo.name }}
            port:
              number: {{ .Values.ErieCanal.services.repo.port }}
{{- end }}
{{- end }}
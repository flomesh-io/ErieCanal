apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.ErieCanal.configmaps.mesh.name }}
  namespace: {{ include "ErieCanal.namespace" . }}
  labels:
    {{- include "ErieCanal.labels" . | nindent 4 }}
data:
  mesh_config.json: |-
    {
      "isManaged": false,

      "images": {
        "repository": "{{ .Values.ErieCanal.image.repository }}",
        "pipyImage": {{ include "ErieCanal.pipy.image.wo-repo" . | quote }},
        "proxyInitImage": {{ include "ErieCanal.proxy-init.image.wo-repo" . | quote }},
        "klipperLbImage": {{ include "ErieCanal.service-lb.image.wo-repo" . | quote }}
      },

      "repo": {
        "rootUrl": {{ include "ErieCanal.repo-service.url" . | quote }}
      },

      "webhook": {
        "serviceName": "{{ .Values.ErieCanal.services.webhook.name }}"
      },

      "ingress": {
        "enabled": {{ .Values.ErieCanal.ingress.enabled }},
        "namespaced": {{ .Values.ErieCanal.ingress.namespaced }},
        "tls": {
          "enabled": {{ .Values.ErieCanal.ingress.tls.enabled }},
          "sslPassthrough": {
            "enabled": {{ .Values.ErieCanal.ingress.tls.sslPassthrough.enabled }},
            "upstreamPort": {{ .Values.ErieCanal.ingress.tls.sslPassthrough.upstreamPort }}
          }
        }
      },

      "gatewayApi": {
        "enabled": {{ .Values.ErieCanal.gatewayApi.enabled }}
      },

      "certificate": {
        {{- if .Values.certManager.enabled }}
        "manager": "cert-manager"
        {{- else }}
        "manager": "archon"
        {{- end }}
      },

      "cluster": {
        "region": "default",
        "zone": "default",
        "group": "default",
        "name": "local"
      },

      "serviceLB": {
        "enabled": {{ .Values.ErieCanal.serviceLB.enabled }}
      }
    }